@{
    ViewData["Title"] = "Home Page";
}

<h4>
    @ViewData["message"]
</h4>

<ol>
    @foreach(string s in (IEnumerable<string>)ViewData["colors"])
    {
        <li>
            @s
        </li>
    }
</ol>